'use client';

const BookmarkDisplay = props=>{
    const handleDelete= (e)=>{
        e.preventDefault();
        
    }

    return(

        <button>Delete</button>
    )
}

export default BookmarkDisplay;